// main.js - Entry point for modular layout builder
import { LayoutBuilder } from "./modules/layoutBuilder.js";

window.addEventListener("DOMContentLoaded", () => {
  // Initialize the modular layout builder for full functionality test
  new LayoutBuilder("layoutContainer");
});
